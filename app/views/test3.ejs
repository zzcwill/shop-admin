<!DOCTYPE html>
<html lang="zh-CN">

<head>
	<!-- <link rel="shortcut icon" href="/images/favicon.ico"/> -->
	<meta name="viewport" content="width=1240,user-scalable=yes">
	<meta name="keywords" content="zzc" />
	<meta name="description" content="zzc" />
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<title>
		<%= title %>
	</title>
	<link rel='stylesheet' href='/css/reset.css' />
	<link rel='stylesheet' href='/css/common.css' />
	<script src="/js/jquery-1.11.1.min.js"></script>
	<script src="/js/common.js"></script>
</head>
<style>
	h1 {
		margin: 20px auto;
		text-align: center;
	}

	h2 {
		margin: 10px auto;
		text-align: center;
	}

	h3 {
		margin: 10px auto;
		width: 302px;
		height: 34px;
		background-color: #000;
	}
</style>

<body>
	<h1>
		<%= title %>
	</h1>

	<h1>接口-menu</h1>
	<h2 id="menu">菜单列表</h2>
	<br />
	<h2 id="userMenu">用户菜单列表</h2>
	<br />
	<h2 id="role">角色列表</h2>
	<h2 id="userRole">用户角色列表</h2>
	<br />


	<script>
		$('#menu').bind('click', function () {

			var apiData = {
				token: token,
				page: 1,
				pageSize: 10
			}

			$.ajax({
				type: "post",
				url: baseUrl + "menu",
				contentType: 'application/x-www-form-urlencoded',
				data: apiData,
				success: function (res) {
					console.log(res);
				},
				error: function () {
				}
			});
		})
		$('#userMenu').bind('click', function () {

			var apiData = {
				token: token,
				page: 1,
				pageSize: 10
			}

			$.ajax({
				type: "post",
				url: baseUrl + "userMenu",
				contentType: 'application/x-www-form-urlencoded',
				data: apiData,
				success: function (res) {
					console.log(res);
				},
				error: function () {
				}
			});
		})

		$('#role').bind('click', function () {

			var apiData = {
				token: token,
				page: 1,
				pageSize: 10
			}

			$.ajax({
				type: "post",
				url: baseUrl + "role",
				contentType: 'application/x-www-form-urlencoded',
				data: apiData,
				success: function (res) {
					console.log(res);
				},
				error: function () {
				}
			});
		})
		$('#userRole').bind('click', function () {

			var apiData = {
				token: token,
				page: 1,
				pageSize: 10
			}

			$.ajax({
				type: "post",
				url: baseUrl + "userRole",
				contentType: 'application/x-www-form-urlencoded',
				data: apiData,
				success: function (res) {
					console.log(res);
				},
				error: function () {
				}
			});
		})


	</script>
</body>

</html>